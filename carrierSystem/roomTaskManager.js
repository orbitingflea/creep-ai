// @ts-check

// define a prototype for the room task manager
var RoomTaskManager = function(room) {
    this.room = room;
    this.taskList = [];
};

RoomTaskManager.prototype = {
    // add a new task
    
};
