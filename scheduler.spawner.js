var schedulerSpawner = {
    // 单房间的 creep spawning planner
    // 可以接收 request，并存储一个 request 的列表，按照优先级来排序
    // 当房间中的能量足够 spawn 当前最重要的 creep 时，会执行
    // request 有一个属性 name，相同名字的 request 将会被覆盖

    
}

module.exports = schedulerSpawner;
